# Wireless ATE Agent 版本特性

本文档简要记录 Wireless ATE Agent 自有 UI 版本的新增能力、体验改进、问题修复和关键依赖升级，供用户了解升级内容，也供后续开发定位版本边界。

产品 UI 版本从 `0.1.0` 开始独立记录；此前的 pi-web 上游版本历史可通过 Git 提交查询。最新版本始终排在最前。

## 未发布

- **改进**：新会话首屏集中展示五场景 Agent 的职责、工作方式与默认交付，并统一侧栏、工作区和文件面板的蓝灰视觉体系。
- **新增**：建立需求、设计、开发、联调、测试五场景 Agent 框架；新会话固定场景并加载对应 `AGENTS.md` 与内置 Skill 路径，历史会话按场景展示。
- **新增**：提供可校验的 `bundled-resources` 资源清单和 Skill 预留目录，Windows 打包时自动复制到安装包；“技能”面板按场景显示这些只读内置 Skill。
- **改进**：打包前后使用实际 Pi 运行时逐场景校验内置 Skill，并遍历全部支撑文件；格式错误、加载告警、同场景重名、路径越界或文件系统链接会直接阻止生成安装包。
- **改进**：用户可见品牌升级为 Wireless ATE Agent，同时保留 `ATE-Agent.exe`、安装目录、注册表键、CLI 和环境变量等兼容标识。
- **兼容**：旧会话保留在“未分类历史”，不自动猜测场景；旧品牌快捷方式在覆盖安装与卸载时会被清理。

## 0.1.5 - 2026-08-25

- **新增**：安装包内置 MCP Adapter 及配套 Skill，新安装无需再通过 npm 单独安装；升级后会移除用户全局安装的旧 Adapter，并保留 MCP 配置、集成运行时、缓存和 OAuth 凭据。
- **新增**：提供只读工具预设、跟随系统的主题模式、浏览器完成通知、扩展状态组件，以及回复写入文件列表和图片点击预览。
- **改进**：工作区会记住最近会话、未读运行状态、文件树折叠状态和文件预览位置；Markdown 支持 YAML 元数据卡，模型配置支持请求头、兼容参数与独立价格编辑。
- **改进**：流式链路支持 Pi 0.84 增量事件、工具参数生成与执行进度，并强化刷新、断网和跨标签页后的会话恢复。
- **修复**：拒绝有歧义的裸模型范围，安全处理 Provider 与模型加载失败；统一 Windows 项目标识并隔离各项目命令环境，改善移动端输入与工具栏布局。
- **修复**：兼容 Next 16.3.1 内置文件追踪，阻止 Windows 跨盘构建将用户目录纳入安装包追踪，避免打包内存溢出和本机文件误收集。
- **依赖**：合入 pi-web `0.8.7` 至 `0.8.9` 的代码增量，Pi SDK 升至 `0.84.2`，Next.js 升至 `16.3.1`；继续使用 ATE Agent 自有品牌、图标、默认目录和面板拖拽实现。

## 0.1.4 - 2026-08-01

- **新增**：Windows 目录选择器可直接切换可用盘符；技能面板和斜杠命令面板会区分已启用与休眠技能。
- **改进**：插件输入支持粘贴安装命令，扩展对话框可复用完成提示音；iOS PWA 更准确地适配安全区、地址栏和软键盘高度。
- **修复**：显式的新会话模型与思考级别会正确保存，API Key 保存不再因凭据锁重入而挂起，Safari 可正常加载同源 DOCX 预览。
- **修复**：提示完成后短暂复用事件流以接收扩展后续消息；服务关闭、会话删除和信任重载会正确释放 AgentSession。
- **性能**：减少高频 Agent 消息触发的空闲计时和运行状态维护，并避免重复打开会话文件。
- **上游**：合入 pi-web `0.8.6` 代码增量；继续使用 ATE Agent 自有品牌、图标和面板拖拽实现，Pi SDK 保持 `0.83.0`。

## 0.1.3 - 2026-07-31

- **新增**：支持通过 `PI_WEB_PASSWORD` 为网页和 API 启用可选的 HTTP Basic Auth。
- **改进**：模型范围与 Pi CLI 保持一致，支持 `enabledModels` 通配、模糊匹配和思考等级固定，并在配置无法匹配时显示提示。
- **改进**：双认证 Provider 根据实际认证能力和凭据类型显示，切换 OAuth/API Key 后同步刷新两个列表。
- **修复**：模型配置改为原子写入，凭据删除增加文件锁和类型校验，降低并发写入或过期界面误删新凭据的风险。
- **修复**：可见标签页使用轻量轮询同步运行状态，并等待整轮提示真正结束后再关闭事件流，避免后台页面占用连接或提前漏掉后续事件。
- **上游**：合入 pi-web `0.8.5` 代码增量；继续使用 ATE Agent 自有品牌、图标和面板拖拽实现。

## 0.1.2 - 2026-07-31

- **新增**：提供 AteTest、Ascend 内部模型入口；默认系统提示词可随界面语言切换中文或英文。
- **改进**：桌面端左右面板支持拖动调宽、贴边收起和宽度恢复；简化 Skill 与插件的添加入口和交互。
- **修复**：模型配置保存后可用模型列表不同步；PWA 继续使用旧前端缓存；流式输出在用户查看历史消息时错误抢占滚动位置。
- **修复**：打包前校验实际安装的 Agent 版本，避免依赖未刷新时启动器仍显示旧版本。
- **依赖**：合入 pi-web `0.8.4` 的依赖升级，Agent 运行时及其 Pi SDK 核心包由 `0.82.1` 升至 `0.83.0`。

## 0.1.1 - 2026-07-30

- **新增**：支持发现 Provider 上游模型、从 models.dev 补全模型信息和价格，以及筛选和切换 Git Worktree。
- **新增**：加入长会话小地图和 PWA 安装、离线页能力，改善长对话导航与桌面使用体验。
- **改进**：优化模型选择、Markdown/Mermaid 展示、会话导出及扩展消息流处理。
- **修复**：统一网页、PWA 与 Windows 桌面端的 ATE Agent 品牌图标。

## 0.1.0 - 2026-07-30

- **首个 ATE 版本**：基于 pi-web `0.8.2` 建立独立 UI 版本线，统一 ATE Agent 品牌和默认工作目录。
- **新增**：提供包含 Node.js 与 Pi 运行时的 Windows 便携包、NSIS 安装程序、状态主界面、托盘和单实例启动器。
- **新增**：支持上传 ZIP 安装 Skill 和带可选 MCP 运行时的集成包，并完善校验、安装、更新、卸载闭环。
- **改进与修复**：完善 Windows 进程树停止、覆盖升级、目录布局和运行时资源；修复安装图标、Skill 目录提示与上下文压缩失败反馈。
